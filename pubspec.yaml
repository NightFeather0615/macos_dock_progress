name: macos_dock_progress
description: "Interacting with macOS dock icon progress bars in Flutter."
version: 0.0.1
homepage: https://github.com/NightFeather0615/macos_dock_progress

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      macos:
        pluginClass: MacosDockProgressPlugin
